{
    "buildCommand": "prisma generate && npm run build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": [
        "iad1"
    ],
    "env": {
        "NEON_URL": "@database_url",
        "DIRECT_NEON_URL": "@direct_url",
        "JWT_SECRET": "@jwt_secret"
    }
}