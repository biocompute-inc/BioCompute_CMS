{
    "buildCommand": "prisma generate && npm run build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": [
        "iad1"
    ],
    "env": {
        "NEON_URL": "NEON_URL",
        "DIRECT_NEON_URL": "DIRECT_NEON_URL",
        "JWT_SECRET": "JWT_SECRET"
    }
}